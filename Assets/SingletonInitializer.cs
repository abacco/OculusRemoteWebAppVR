using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SingletonInitializer : MonoBehaviour
{
    //public GameManager playerHealthManagerPrefab;
    //public GUIMANAGER textManagerPrefab;

    //private void Awake()
    //{
    //    InitializeSingletons();
    //}

    //private void InitializeSingletons()
    //{
    //    GameManager playerHealthManager = FindObjectOfType<GameManager>();
    //    if (playerHealthManager == null)
    //    {
    //        playerHealthManager = Instantiate(playerHealthManagerPrefab);
    //    }

    //    GUIMANAGER textManager = FindObjectOfType<GUIMANAGER>();
    //    if (textManager == null)
    //    {
    //        textManager = Instantiate(textManagerPrefab);
    //    }
    //}
}
