using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class logicagioco : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //single player dove noi miglioriamo il personaggio e il single player è andare in missione nei boschi poi si cambia man mano
          //  il multiplayer è contro gli altri giocatori stile clash of clans e idle

        // cambiare fame e sete con salute e mana

        // mappa dei villaggi, single player ad ondate e fasi, alla fase 10/10 si passa al prossimo villaggio
        // concludere il rush prima della notte
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
